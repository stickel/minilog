<%= render :partial => 'admin/base/header' %>
	<%# @posts.each do |p| %>
	<%- unless @posts.nil? -%>
	<!-- TODO: post subnav here -->
	<%= render :partial => 'admin/posts/list', :locals => {:posts => @posts} %>
	<%- else -%>
	<p>There are currently no posts. You should think about <%= link_to "writing one", new_admin_post_path %>.</p>
	<%- end -%>
<%= render :partial => 'admin/base/footer' %>